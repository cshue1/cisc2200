#include "btree.h"

btree::btree()
{
    root       = NULL;
    height     = 0;
    numOfNodes = 0;
}

btree::~btree()
{
}

bool btree::insert(int key)
{
    node *temp;
    temp = new node;
    if (temp == NULL)
	return false;
    temp -> key_value = key;
}
